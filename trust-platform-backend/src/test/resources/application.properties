# =========================================
# TEST PROPERTIES
# =========================================
spring.application.name=trust-platform-backend
server.port=8080
server.forward-headers-strategy=framework

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/trustplatform?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata&characterEncoding=UTF-8&useUnicode=true
spring.datasource.username=root
spring.datasource.password=Rohith@358
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# Flyway
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1

# JWT
app.jwt.secret=kvg_trust_super_secure_jwt_secret_2026_kvg_trust_super_secure_jwt_secret_2026
app.jwt.expiration=900000
jwt.refresh-expiration=2592000000
app.verification.expiration=86400000

# Rate Limiting
rate-limit.auth.capacity=10
rate-limit.auth.refill-tokens=10
rate-limit.auth.refill-seconds=60
app.cookie.secure=false

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mock_username
spring.mail.password=mock_password
app.mail.from.name=KVG Sai Charitable Trust
app.mail.from.email=kvgshanmukhsaitrust@gmail.com

# Integrations
razorpay.key-id=dummy
razorpay.key-secret=dummy
razorpay.webhook-secret=dummy
app.receipts.storage-path=./receipts
cloudinary.url=cloudinary://12345:abcde@dummy
app.frontend.url=http://localhost:5173

# OAuth
spring.security.oauth2.client.registration.google.client-id=mock-google-client-id
spring.security.oauth2.client.registration.google.client-secret=mock-google-client-secret
spring.security.oauth2.client.registration.google.scope=profile,email

# PAN Encryption Key (Test/Development)
app.crypto.pan-key=dummy_pan_key_must_be_32_bytes_long!
app.environment=test
